spring:
  application:
    name: ${APP_NAME}
  
  profiles:
    active: ${APP_ENV}

dotenv:
  enabled: true

app:
  jwt-secret: ${JWT_SECRET}
  jwt-expiration-milliseconds: ${JWT_EXPIRATION}

sentry:
  dsn: ${SENTRY_DSN}
  logging:
    mdc-tags: "traceId"